<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="RESTInMAUI.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:RESTInMAUI">

    <ContentPage.BindingContext>
        <local:MainPageViewModel />
    </ContentPage.BindingContext>

    <VerticalStackLayout
        HorizontalOptions="Center"
        Spacing="10"
        VerticalOptions="Center">
        <Button Command="{Binding AddAllUserCommand}" Text="Load All User" />
        <Button Command="{Binding AddSingleUserCommand}" Text="Load Single User" />
        <Button Command="{Binding CreateNewUser}" Text="Create new User" />
        <Button Command="{Binding EditUserCommand}" Text="Update User" />
        <Button Command="{Binding DeleteUserCommand}" Text="Delete User" />
    </VerticalStackLayout>

</ContentPage>
